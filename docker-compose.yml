# Production stack.
#
# Runs prebuilt images with no bind mounts and no auto-reload. The API image
# bakes the built SPA in and serves it directly, so production has no separate
# frontend service and no web port — the dashboard is on ${API_PORT}.
#
# For local development, layer the dev override on top:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up
#
# Host ports are unique per solution so all four can run side by side.
# This one: Postgres 5432, API 8000. See .env.example.

services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-copilot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-copilot}
      POSTGRES_DB: ${POSTGRES_DB:-copilot}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-copilot} -d ${POSTGRES_DB:-copilot}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    # Defaults to the public GHCR image. Point API_IMAGE at the Gitea registry
    # to run the internally built one instead — see .env.example.
    image: ${API_IMAGE:-ghcr.io/loryanstrant/m365copilot-usage-reporter/api:latest}
    build:
      context: .
      target: api
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://copilot:copilot@db:5432/copilot}
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${API_PORT:-8000}:8000"

  worker:
    image: ${WORKER_IMAGE:-ghcr.io/loryanstrant/m365copilot-usage-reporter/worker:latest}
    build:
      context: .
      target: worker
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://copilot:copilot@db:5432/copilot}
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
