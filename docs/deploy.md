# Deploying the M365 Copilot Usage Reporter

There are two supported paths:

1. **One-click "Deploy to Azure" button** — pulls prebuilt public images, no build tools required.
2. **`azd up`** — builds from source and provisions the same infra.

---

## 1. One-click button

### For end users (deploying an existing published build)

1. Click **Deploy to Azure** in the [README](../README.md).
2. Pick a subscription and **create/choose a resource group**.
3. Set a **dashboard admin password** (everything else has sensible defaults).
4. **Review + create.** Provisioning takes ~5–10 minutes.
5. Open the deployment's **Outputs → `dashboardUrl`** and sign in with `admin` + your password.
6. Go to **Settings** and connect Microsoft Graph (see [After deployment](#after-deployment)).

What gets created (all in your resource group):

| Resource | Purpose |
| --- | --- |
| Container Apps environment | Hosts the api + worker |
| `…-api-…` container app | Public dashboard + API (serves the React bundle) |
| `…-worker-…` container app | Background ingestion/scheduler |
| PostgreSQL flexible server | Stores prompts, users, licenses |
| Log Analytics workspace | Container logs |

Secrets you never have to handle: the **PostgreSQL password**, the **Fernet key** (encrypts the
Graph client secret at rest), and the **JWT signing key** are all generated by the template.

### Choosing a region and database size

- **Region:** the portal's region picker only lists regions that support both Container Apps and
  PostgreSQL Flexible Server. Prefer a full-service region (e.g. `australiaeast`, `eastus`,
  `westeurope`).
- **Resource name prefix:** lowercase, start with a letter, keep it short (≤ 12 chars). Container
  App names must be ≤ 32 characters and the template appends `-worker-` plus an 8-character unique
  token, so an over-long prefix is rejected. `copilot` is a safe default.
- **Database version / size:** the **Advanced** tab exposes the PostgreSQL version and compute
  size. Defaults (v16, Burstable `Standard_B1ms`) are the cheapest and fine for most tenants.

> **Troubleshooting — `The value of the 'Version' should be in: []`**
> The empty list `[]` means the PostgreSQL capabilities API returned nothing for your subscription
> in that region, so Azure can't validate the version. It is **not** the version number, the compute
> size, or the template (the Azure retail catalogue can still list the SKU as "available"; catalogue
> pricing is not the same as your subscription's provisioning eligibility). Two causes, in order of
> likelihood:
>
> 1. **The `Microsoft.DBforPostgreSQL` resource provider isn't registered** on the subscription (or
>    is blocked by policy). This fails in *every* region. Fix: Azure portal → your **Subscription** →
>    **Resource providers** → search `Microsoft.DBforPostgreSQL` → **Register** (takes a minute),
>    then redeploy. CLI equivalent: `az provider register --namespace Microsoft.DBforPostgreSQL`.
> 2. **Your subscription can't provision a flexible server in that specific region.** Fix: pick a
>    different region (e.g. `australiaeast`, `eastus`, `westeurope`).
>
> The portal form now checks this live and warns you on the **Basics** tab as soon as you pick a
> region, so you don't have to wait for the deployment to fail.

> **Troubleshooting — `Invalid ContainerApp name … must be between 2 and 32 characters`**
> Your name prefix is too long. Use a shorter, lowercase prefix that starts with a letter
> (`copilot` works). The template now caps the unique suffix so a ≤ 12-char prefix always fits.

### For maintainers (one-time, so the button works for everyone)

The button can only pull **public** images. Publish them once:

1. Push this repository to `github.com/loryanstrant/M365Copilot-Usage-Reporter`.
2. The **Publish container images** workflow (`.github/workflows/publish-images.yml`) runs on push
   to `main` (and on `v*` tags) and pushes two images to GHCR:
   - `ghcr.io/loryanstrant/m365copilot-usage-reporter/api`
   - `ghcr.io/loryanstrant/m365copilot-usage-reporter/worker`
3. **Make both packages public** (one time): repo **Packages** → open each package →
   **Package settings** → **Change visibility** → **Public**. Azure Container Apps then pulls them
   anonymously with no registry credentials.
4. Tag a release (`git tag v1.0.0 && git push --tags`) if you want the button to offer a pinned
   version instead of `latest`.

---

## 2. azd (build from source)

```powershell
azd env set POSTGRES_ADMIN_PASSWORD "<strong-password>"
azd env set FERNET_KEY "<fernet-key-or-any-string>"
azd env set SECRET_KEY "<random-secret>"
azd env set ADMIN_PASSWORD "<admin-password>"
azd up
```

Optional database overrides (defaults: v16, Burstable `Standard_B1ms`). Change these only if you
specifically want a different version or size — note they will **not** fix a
`The value of the 'Version' should be in: []` error, which is a region/subscription eligibility
issue (deploy to a region where your subscription can create a PostgreSQL flexible server, e.g.
`australiaeast`, `eastus`, `westeurope`):

```powershell
azd env set POSTGRES_VERSION "16"
azd env set POSTGRES_SKU_NAME "Standard_D2ds_v5"
azd env set POSTGRES_SKU_TIER "GeneralPurpose"
```

`azd down` tears everything down.

---

## After deployment

The reporter needs an Entra app registration with **application** permissions:

- `AiEnterpriseInteraction.Read.All`
- `Directory.Read.All`

Grant admin consent, create a client secret, then in the dashboard:

1. **Settings** → a **guided first-run wizard** walks you through creating the app
   registration (with copy buttons and an optional one-shot PowerShell script). Enter the
   **Tenant ID**, **Client ID**, **Client Secret**, confirm the Copilot SKU id(s), and
   (optionally) a schedule + report-access group.
2. **Test connection** → **Run ingest now** (or start a **Backfill** for history).
3. Explore the dashboard.

The client secret is encrypted at rest with the Fernet key before it touches the database.

---

## Entra single sign-on (optional)

By default the dashboard uses a single admin password. You can additionally let licensed users
sign in with their **work account** via Azure Container Apps **Easy Auth** — no code or extra
services, and it's free. Administration stays behind the password; SSO users get a read-only
**viewer** role.

**Enable it at deploy time** by setting these template parameters (all default off/empty):

| Parameter | Value |
| --- | --- |
| `enableEntraAuth` | `true` |
| `entraClientId` | Application (client) ID of the sign-in app registration (you can reuse the reporter's own) |
| `entraClientSecret` | A client secret for that app registration |
| `entraTenantId` | Defaults to the deployment tenant |

**Two one-time steps on the app registration** (Easy Auth is the relying party):

1. **Redirect URI** — after the deploy finishes, copy the deployment output
   `entraRedirectUriToRegister` (looks like `https://<app>.azurecontainerapps.io/.auth/login/aad/callback`)
   and add it under the app registration's **Authentication → Web → Redirect URIs**.
2. **Group claim (recommended if you set a report-access group)** — under **Token configuration →
   Add groups claim**, include **Security groups**. This lets the group gate work straight from the
   token. Without it, the reporter falls back to an app-only Graph `checkMemberGroups` call using the
   permissions it already has.

Once enabled, the sign-in page shows a **"Sign in with Microsoft"** button, and returning users are
signed in silently. If the configured **report access group** is set, only its members are admitted;
everyone else is refused (fail-closed).

To enable it on an already-deployed instance, redeploy the template with the parameters above, or add
the authentication config to the api container app in the portal (**Authentication → Add identity
provider → Microsoft**, unauthenticated access **allowed**).

---

## Updating a deployment

- **New build:** re-run the publish workflow (or push to `main`). Restart the container apps, or
  redeploy the template with the new `imageTag`, to pick up the new image.
- **Move the database:** point `DATABASE_URL` at any PostgreSQL — no code change.
